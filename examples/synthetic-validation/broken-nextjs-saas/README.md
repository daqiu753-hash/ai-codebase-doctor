# Synthetic Broken Next.js SaaS

This is simulated validation data.
These fixtures are synthetic projects.
This is not evidence of real-world adoption.

This fixture models an AI-generated Next.js SaaS repo that looks ready but has broken project reality.

## Setup

```bash
npm install
npm run dev
```

The app uses an AI customer insights feature and a database connection.

## Database

Run the Prisma migration before starting the app:

```bash
npx prisma migrate dev
```

